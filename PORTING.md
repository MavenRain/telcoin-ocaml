# Porting map: telcoin-network (Rust) → telcoin-ocaml

This file is the re-sync contract. Each row maps a Rust source area to the
OCaml module that models it, so the OCaml can be diffed against upstream as the
Rust node moves. The port is **protocol-first**, not file-for-file: the OCaml is
organised around the DAG-consensus protocol itself, with a pure functional core
and an imperative shell, so one OCaml module may correspond to several Rust
files and vice versa.

Upstream reference: `~/Documents/telcoin-network`, a Narwhal + Bullshark DAG
consensus layer over a reth (Ethereum) execution client, tag context around
`v1.11.3` reth deps.

## Status legend

- **done** — implemented and tested in this milestone
- **partial** — modelled, with documented simplifications
- **planned** — signature/plan exists, implementation pending

## Foundation

| OCaml module | Rust source | Status | Notes |
|---|---|---|---|
| `tn_codec` (`Bcs`) | `bcs` crate (external) | done | Canonical BCS encoder/decoder combinators. 24 golden-vector conformance checks against the BCS spec. Digest/signature byte-compatibility with the Rust node depends on this. |
| `tn_std` (`Nonempty`, `Prng`) | — (port infrastructure) | done | `Nonempty` gives total `head`/`last` for quorum-bearing collections; `Prng` is SplitMix64 for replayable simulator latency. |
| `tn_crypto` (virtual) | `crates/types/src/crypto` | done | The single crypto seam. Interface only; implementation chosen at link time. |
| `tn_crypto_stub` | — (simulation only) | done | Deterministic, **forgeable** stub: BLAKE2s-256 digest, structural signatures. Exercises every path without a native dependency. |
| `tn_crypto_blst` | `crates/types/src/crypto`, `crates/config/src/keys.rs` | planned | Real BLAKE3 + BLS12-381 min-sig via `bls12-381`/`blst`. DST `BLS_SIG_BLS12381G1_XMD:SHA-256_SSWU_RO_NUL_`, 96-byte G2 pubkeys, 48-byte G1 sigs, infinity-point default, subgroup checks. |

## Core types (`crates/types`)

| OCaml module | Rust type(s) | Status | Notes |
|---|---|---|---|
| `Tn_types.Round` | `Round = u32` (`primary/mod.rs`) | done | Abstract; parity as a variant, saturating GC arithmetic. |
| `Tn_types.Leader_round` | (the `round % 2 == 0` asserts) | done | Even-and-≥2 by type; makes `LeaderSchedule::leader` total. |
| `Tn_types.Units.Epoch` | `Epoch = u32` (`committee.rs`) | done | |
| `Tn_types.Units.Stake` | `VotingPower = u64` | done | Always `one` per authority (`EQUAL_VOTING_POWER`). |
| `Tn_types.Units.Timestamp` | `TimestampSec = u64` | done | Whole seconds; enters digest pre-images. |
| `Tn_types.Units.Duration` | (tokio `Interval` delays) | done | Simulator milliseconds; never hashed. |
| `Tn_types.Units.Sequence_number` | `SequenceNumber` / leader nonce | done | `(epoch << 32) | round`; split recoverable for the reputation schedule counter. |
| `Tn_types.Units.Worker_id` | `WorkerId = u16` | done | One worker per validator in current protocol. |
| `Tn_types.Units.Address` | `alloy Address` | done | 20-byte execution address. |
| `Tn_types.Units.Base_fee` | `base_fee_per_gas: u64` (`worker/sealed_batch.rs`), `MIN_PROTOCOL_BASE_FEE` (alloy-eips 1.8.3 `eip1559/constants.rs:21`) | done | Raw u64 bits in `int64`, unsigned compare/render; `min_protocol` = 7 wei, the genesis floor and `Batch::default()`'s fee. |
| `Tn_types.Authority_id` | `AuthorityIdentifier` (`committee.rs`) | done | `hash(protocol_key)`; byte order drives all committee traversal. `zero` = Rust `Default`. |
| `Tn_types.Digests.*` | `digest_newtype!` macro outputs | done | `Header_digest`, `Batch_digest`, `Sub_dag_digest`, `Output_digest`; distinct abstract types + domain tags. |
| `Tn_types.Authority` | `Authority`/`AuthorityInner` | done | |
| `Tn_types.Committee` | `Committee`/`CommitteeInner` | done | Smart constructor enforces size ≥ 2, no dup keys; derives 2f+1 / f+1. Bitmap index = id-sorted position. |
| `Tn_types.Batch` (+ `Batch.Sealed`) | `Batch`/`SealedBatch` (`worker/sealed_batch.rs:61-200`) | done | FROZEN BCS digest preimage (bcs 0.1.6 layout: uleb128 tx count, per-tx length + raw EIP-2718 bytes, epoch u32 LE, length-prefixed 20-byte beneficiary, base fee u64 LE, worker u16 LE), pinned by cross-language golden vectors V1-V7; `received_at` digest- and wire-excluded (serde skip), codec decode resets it to `None` and rejects trailing bytes; fork-blind `max_batch_gas` 30M / `max_batch_size` 1M. **Deliberate divergence:** digest = domain-tagged stub hash until `tn_crypto_blst`, which must swap in bare BLAKE3 AND drop the tag for byte-compat (the blake3 hexes ride inert in the vectors). **Deferred:** the `SealedBatch` wire codec (no oracle vector minted yet); `Sealed.claim` pairs an unverified digest exactly as Rust's `SealedBatch::new`, caught only by validator rule 1. |

## Vertices (`crates/types/src/primary`)

| OCaml module | Rust type(s) | Status | Notes |
|---|---|---|---|
| `Tn_vertex.Intent` | `IntentMessage`/`Intent` | done | 3-byte domain prefix `[2;0;1]`; closed variant. |
| `Tn_vertex.Header` | `Header`/`HeaderInner`/`HeaderBuilder` (`header.rs`) | partial | Digest cached at construction. **Omitted for the slice:** `latest_execution_block: BlockNumHash` (execution coupling) — add before wire-compat. |
| `Tn_vertex.Vote` | `Vote`/`VoteInfo` (`vote.rs`) | done | Signs the intent-wrapped header digest. |
| `Tn_vertex.Certificate` | `Certificate` + `SignatureVerificationState` (`certificate.rs`) | done | Verified-by-construction. The 5-state enum collapses into the type; only genesis-vs-aggregate is retained. `signed_authorities` `RoaringBitmap` → `Authority_id.Set`. |

## Consensus (`crates/consensus`) — parts 1, 2 and 3 done

| OCaml module | Rust source | Status | Notes |
|---|---|---|---|
| `Tn_consensus.Vote_aggregator` | `aggregators/votes.rs` (`VotesAggregator`) | done | Claims an author's slot on first sight (before validating, so a rejected vote burns the author for this header, matching Rust's `authorities_seen`), then validates (wrong header, non-member, bad signature); only accepted votes count toward quorum. `add` always returns the advanced state plus a result, and certifies via `Certificate.assemble` the moment accepted signers reach 2f+1. Fresh per proposal. Errors reported in `Certificate.error`. |
| `Tn_consensus.Parent_aggregator` | `aggregators/certificates.rs` (`CertificatesAggregator`) | done | Per-round parent accumulator. A `seen` author set gives equivocation protection and a **weight that never resets** (only added to); a `pending` buffer holds certificates since the last release and is drained on each release, so each release carries the delta, matching Rust's `drain(..)`. Releases at 2f+1 and re-releases on every later straggler; a post-quorum duplicate author releases nothing. The proposer appends deltas, so a cumulative re-release would double-count parent stake there. |
| `Tn_consensus.Dag` | `consensus/state.rs` (`ConsensusState` DAG half) | done | `Round.Map` of `Authority_id.Map` of `Certificate.t` plus a `Header_digest`-keyed secondary index. Equivocation guard (one certificate per (round, author); same digest idempotent), parent existence check with the round-1 genesis-parent skip (`round <= gc_round + 1`), GC horizon (`committed_round - gc_depth`, purge on `update`). Part 2 adds the `gc_depth` and `last_committed_round` read accessors the commit traversal needs; a parent-check-disabled `insert_recovered` is reserved (in the `.mli`) for the storage/recovery chunk. |
| `Tn_consensus.Reputation_scores` | `tn-types` `reputation.rs` (`ReputationScores`) | done | Per-authority leader-support tallies for one schedule window plus a `final_of_schedule` flag. Rust's two asserts (full committee coverage; `total_authorities == size`) become smart-constructor invariants: `fresh` seeds every member, `bump` (`Map.update … (Option.map succ)`) can only touch existing keys. `by_score_desc` breaks ties by **descending** id, fixing the swap-table carve order. |
| `Tn_consensus.Leader_schedule` | `LeaderSchedule`/`LeaderSwapTable` | done | Round-robin `(r/2 - 1) mod size` over the id-sorted committee, plus a reputation-derived swap table (bad = low scorers within a descending std-ceiling capped at `size * pct / 100`; good = high scorers). Three Rust panics vanish into types (odd-round `Leader_round.t`, non-empty good list, `Threshold.of_percent ∈ [0,33]`). **Documented divergence:** the swap RNG is the house SplitMix64 `Prng` seeded by the queried round, not Rust's ChaCha12; determinism and the good/bad sets are identical, the concrete pick differs only when >1 good node exists — deferred behind the `Prng` seam. |
| `Tn_consensus.Sub_dag` | `CommittedSubDag` (`primary/output.rs`) | done | One commit's flattened certificates as bare headers (ascending round, leader last, so `Nonempty.last` is total), running scores, monotone commit timestamp (raw `stored` for the digest, zero-fallback getter for the view — kept as two functions), and signature-derived randomness. `preimage : t -> string` is factored out and documented as the frozen wire-compat byte layout (header digests ++ BCS scores ++ 8-byte-LE timestamp ++ randomness); `digest` domain-tags and hashes it. |
| `Tn_consensus.Bullshark` | `bullshark.rs` (`process_certificate`, `commit_leader`, `order_leaders`, `linked`) + `utils.rs` (`order_dag`) | done | The commit rule. `outcome = Committed of Sub_dag.t Nonempty.t \| No_commit of reason` makes Rust's commit-with-empty-list override unrepresentable; `Schedule_changed` is an internal bounded recursion that never escapes. Replays the Rust `bullshark_tests` scenarios (commit-one, round-robin, missing/dead leaders, weak/not-enough support, GC, reputation-reset cadence, and both schedule-change cases incl. the singleton-good swap) as alcotest cases. Two upstream-test expected multisets were corrected where they assumed a watermark of 6 for round-6 siblings that are only committed at 5. |
| `Tn_consensus.Proposer` | `proposer.rs` (`Proposer` task) | done | Pure Mealy machine `step : t -> now -> input -> t * action list`. Timer **generation counters** kill stale re-arms (critic HIGH): every re-arm bumps a monotone `gen`, a `Timer_fired` with a stale `gen` is discarded — the pure stand-in for `Interval::reset`. Propose condition `enough_parents && (max_timed_out \|\| (advance_round && (enough_digests \|\| min_timed_out)))`; parents jump/extend/ignore by round comparison; sticky timeout flags cleared only on a proposal; `process_committed_headers` digest re-queue (skipped headers' payloads prepended, oldest-round-first). **Timing chunk (ported):** the proposer now holds the `Leader_schedule` (refreshed by the Node after each commit, the pure stand-in for Rust's shared `Arc<RwLock<_>>`). *Leader fast path* — proposing the round before its own even leader round halves the max header delay and collapses the min to zero (`calc_max_delay`/`calc_min_delay`), chosen at re-arm time under the current generation. *Readiness gate* — the `advance_round` conjunct withholds an early (pre-max-timeout) proposal until `ready` holds (even round: the round's leader certificate is in the held parents via `update_leader`; odd round: the leader votes have settled via `enough_votes`, f+1 for it or a 2f+1 quorum against); the max deadline overrides it, so liveness is unchanged. The gate is recomputed from `ready` at the end of `apply_parents` and cleared on every proposal, mirroring Rust exactly. **Still deferred:** the equivocation-guard re-emit of a stored header is present for the recovery chunk but unreachable in forward operation, so `step` needs no error result. |
| `Tn_consensus.Voter` | `certifier.rs` + `network/handler.rs` (`RequestHandler::vote`) | done | Pure `vote : t -> dag -> now -> Header.t -> t * decision`. **Vote-once** keyed by author (latest vote): identical re-request → `Recast` the stored vote, a different header for a voted round → `Reject Equivocating_header`, a lower round → `Reject Already_voted_higher`. Parents resolved genesis-first then the DAG (the round-1 genesis-parent rule falls out), checked for one-round-below layering, `2f+1` quorum over distinct origins, and `created_at` monotonicity (inclusive, equality allowed). A Byzantine peer is protocol-normal, so **every outcome is a `decision`, never an error** — the genuine invariant break (two conflicting *certificates*) is the DAG's, surfaced at the Node. **Timing chunk (ported):** the future-timestamp check now applies Rust's `max_header_time_drift_tolerance` (whole seconds, default 1): a header stamped up to one second ahead of `now` is voted (Rust waits out the small drift then votes; the pure equivalent is an immediate accept), beyond that is `Reject Future_timestamp`. Slice-deferred (IO): batch sync, evaluation timeout, execution-result waits — an unresolved parent yields `Need_parents` and the peer re-requests. |
| `Tn_consensus.Node` | `primary.rs` + `consensus/mod.rs` | done | Composes Proposer + Voter + `Vote_aggregator` + per-round `Parent_aggregator`s + `Bullshark` into `step : t -> now -> event -> (t * command list, error) result`. Every intra-node channel collapses into direct composition; only true message crossings are events/commands. **`error` is reserved ONLY for the three DAG invariant breaks** (certificate equivocation, missing parent / parent-round) (critic HIGH): wrong-epoch and below-GC certificates are `Ok` no-ops, invalid/duplicate/superseded votes and equivocating peer *headers* produce a command or nothing. A formed certificate is self-inserted through the same spine as gossip and broadcast; released parent quorums drive the next proposal; committed sub-DAGs are emitted in order and prune the proposer + GC the aggregators. |

## Shell + execution seam + state foundation — done; network, storage — planned

| OCaml module | Rust source | Status | Notes |
|---|---|---|---|
| `Tn_sim.Sim` | tokio runtime + channels (port-only) | done | The deterministic discrete-event simulator that closes the pure core's loop — the runnable vertical slice. Holds one `Node.t` per authority (`Authority_id.Map`), a revealed-certificate set (`Map` keyed by header digest), and a time-ordered event queue (`Map` over `(delivery_ms, seq)`; the monotone `seq` makes the key a total, collision-free order so simultaneous events replay deterministically). `run` is a tail-recursive fold: pop the earliest event, advance the clock, `Node.step`, then interpret each command into future scheduled events — `Broadcast_header` → `Vote_request` to each peer with parents resolved from the revealed set; `Send_vote` → `Vote_received` unicast; `Broadcast_certificate` → reveal + `Certificate_received` to peers; `Arm_timer` → self `Timer_fired` after the span; `Send_missing_parents` answered from the revealed set; `Emit_committed` → the node's output log. The only nondeterminism is per-message latency from a single seeded `Prng`. Pure library: no imperative loops, exhaustive command match, `Option.fold`/`Result.fold` throughout. Observability: `committed`/`commit_count`, `error` (the halting invariant break, if any), `agreement` (pairwise prefix-consistency of the committed logs — a `Diverge` is a safety violation). **Documented slice model:** the clock is milliseconds coarsened to whole seconds for the digest-domain `Timestamp` (a delivery is never earlier than its send, so a voter's clock never trails a header's `created_at`); the shared revealed-certificate set stands in for the fetch protocol a later network chunk adds; honest, fully-connected, reliable delivery only (no Byzantine/loss model yet). |
| `bin/tn_sim` | `bin/telcoin-network` | done | Stands up a 4-validator honest committee (seed 42, 5–50 ms latency, 20 s horizon), runs `Sim` to the horizon, and prints per-node commit counts, the agreement verdict, and node 0's committed leader sequence; exits non-zero on any invariant break, disagreement, or empty output, so it doubles as a smoke test. Default run: all four nodes commit an identical 23-sub-DAG prefix following the round-robin schedule (round 2 → authority 0, 4 → authority 1, …); the leader fast path lets each anticipated leader propose sooner, so more rounds commit within the same horizon than the pre-timing-chunk slice (19). Optional flags `--validators N`, `--seed S`, `--until-s T` (each total, clamped/defaulted) override the run; the latency band lives on `Sim.config`. |
| `Tn_execution.Consensus_block` | `ConsensusHeader` (`types/src/primary/block.rs`) | done | The consensus-chain block: `parent_hash`, one committed `Sub_dag`, a monotone `Number` (ancestor count; genesis anchor 0, first committed block 1), and a cached `digest`. `preimage` is the frozen wire-compat layout — `parent_hash` ++ `sub_dag.digest` ++ 8-byte-LE `number` ++ 32 zero bytes (Rust's unused `extra` `B256::default()`) — exactly the field order of `ConsensusHeader::digest_from_parts`; `digest` domain-tags (`tn:output`) and hashes it. **Genesis-anchor divergence:** Rust's first block links to `ConsensusHeader::default().digest()` (a header wrapping a default certificate); reproducing that needs a `Certificate::default` the port's types forbid (a certificate is proof of a quorum), so `genesis_parent` is a distinct documented constant, byte-compat-deferred with the hash swap. |
| `Tn_execution.Engine` (`ENGINE` / `Noop`) | `executor/src/subscriber.rs` (the output→header fold) | done | The execution seam, widened by the engine chunk so a real executor can satisfy it. `module type ENGINE` now abstracts `config`, `output`, `block`, `height` and `error`, and is `create : config -> t`, `execute : t -> output -> (t * block list, error) result`, `tip : t -> block option`, `height : t -> height`, `error_to_string`. Three of those are what the widening bought: `execute` returns a **list**, because a committed output builds zero blocks (empty and not closing the epoch), one (empty and closing it) or one per flattened batch; `output` is abstract because `Tn_batch.Output.t` is unnameable from `lib/execution` (`tn_batch` sits *above* it); and `height` is abstract because the two implementations number two different chains. `Noop` folds committed sub-DAGs into the `Consensus_block` chain exactly as the Rust subscriber does (`number = prev + 1`, `parent_hash = prev.digest`), instantiating `output = Sub_dag.t`, `height = Consensus_block.Number.t` and a one-element list, and **cannot fail** — its `error` is the uninhabited `Nothing.t` (OCaml's `Void`/Rust's `!`), so `execute` is total and the impossible error branch is discharged by `Nothing.absurd`. The real engine is `Tn_engine.Engine` below. |
| `Tn_sim.Sim` (execution view) | `executor` subscriber wiring | done | The shell derives each node's chain on demand: `executed` / `execution_tip` fold `Noop` over `committed authority`. Pure and outside the event loop, so the honest run stays byte-for-byte deterministic; `bin/tn_sim` prints node 0's chain height and tip. |
| `Tn_state.U256` | `alloy_primitives::U256` (`ruint::Uint<256, 4>`) | done | The 256-bit EVM word, held as its canonical 32-byte big-endian encoding so `String.compare` is the unsigned order and the byte codec (`to_be_bytes`/`of_be_bytes`) is the identity — matching `U256::to_be_bytes` / `U256::from_be_slice`. Wrapping and checked `add`/`sub` via an LSB→MSB carry/borrow fold; `of_int`/`to_int`, hex codec. Multiplication (the low half of a full 512-bit schoolbook product), unsigned division and remainder (binary long division, tracking the bit shifted out of the top so a shifted remainder past `2^256` still compares `>=` the divisor), modular add and multiply reduced from the true 257- and 512-bit intermediates, exponentiation, and the bitwise and shift operations arrived with the EVM ALU chunk. The module stays **mathematically honest**: division and remainder by zero are `None`, never a silently defined value. The EVM's *total* semantics, which map those to zero, live in `Tn_evm.Alu`. `to_int` is the partial inverse of `of_int` the interpreter converts offsets, lengths and jump destinations with. |
| `Tn_state.Storage` | `revm-state` `EvmStorage` (`Address → StorageSlot`) | done | One account's storage: an infinite array of zero words of which a finite set has been written, so `get` is total and the module has no error type. **Canonical:** writing zero *removes* the key rather than storing it, so `equal` is exact content equality, `is_empty` is a constant-time map test rather than a scan, and a contract that clears every slot it wrote leaves a storage `equal` to the one it started from. The storage root (the trie hash committing the map) is now `Tn_evm.Block_roots.storage_root`: a secure trie over the nonzero slots with scalar-RLP values. |
| `Tn_state.Address_word` | `revm-interpreter/src/instructions/utility.rs` (`Address::from_word` / `into_word`) | done | The two total conversions between a 20-byte address and a 256-bit word. `to_word` left-pads with twelve zero bytes and is injective; `of_word` keeps the low twenty bytes and **discards the high twelve without complaint** — a word with dirt above bit 160 is not an error for `BALANCE`, it is a query for the truncated address, so hardening `of_word` into a partial function would halt valid mainnet transactions. It lives in `tn_state` because it must see both `Units.Address` and `U256`, and neither owns the other. |
| `Tn_state.Account` (+ `Nonce`) | account state (`nonce: u64`, `balance: U256`, storage) | done | An account: `Nonce` (saturating u64 counter), `balance` (`U256`), and — since the host seam — its `Storage.t`, with checked `credit`/`debit`, `increment_nonce`, `slot`/`set_slot`, and **two distinct emptiness predicates**. `is_empty` is the literal EIP-161 test (zero nonce, zero balance and no code) — storage is deliberately *not* a conjunct, because EIP-161 does not make it one; it is what state-clearing, the EIP-1052 `EXTCODEHASH` fold-to-zero and the `CALL`-to-empty surcharge use. `is_absent` is `is_empty && Storage.is_empty`, and is what the world state prunes on. `storage_root` needs the state trie and RLP, which the port still defers. Code now lives on the account (`Bytecode.t`, set through `with_code`), so `code_hash` is `Bytecode.hash` — Keccak of the bytes, `KECCAK_EMPTY` for empty code — and `is_empty`'s third conjunct is live. A run itself still deploys no code (that is `CREATE`), so contracts reach the state at genesis or through `set_account`. `storage_root` is now computed by `Tn_evm.Block_roots.storage_root`. |
| `Tn_state.World_state` | the execution state map (`Address → Account`) | done | An address-keyed `Map`, total on addresses (an absent address reads `Account.empty`) and total again at the slot level (`storage` of a slot of a nonexistent account is zero). Storage lives *inside* the account entry rather than in a second address-and-slot map, so orphan storage is not representable and `remove_account` cannot forget half its job. Kept canonical so `equal` is exact content equality and two nodes that applied the same transactions from the same genesis hold identical states (the state-level corollary of consensus agreement). **The pruning predicate is `Account.is_absent`, not `Account.is_empty`.** The distinction is load-bearing: `is_empty` ignores storage (correctly — EIP-161 ignores it), so pruning on it would erase an `SSTORE` into a zero-nonce zero-balance account and `equal` would stop being exact, since two states differing in that slot would compare equal. `is_absent` is strictly stronger and covers every field `Account.equal` compares, which is exactly what canonicity needs; on a real chain the two agree on every account that can exist (reaching storage needs `SSTORE`, which needs code, which falsifies `is_empty`), and this port errs toward keeping the account. `of_alloc` is the genesis funding (`GenesisAccount::with_balance`) and is the one constructor that has fallen behind `Account.t` — it cannot express pre-populated genesis storage. The trie state root and the agreement oracle are now `Tn_evm.Block_roots.state_root` and `Block_roots.agree` (one library up, since `tn_state` sits below `tn_trie`); `equal` stays the exact structural equality canonicity needs. |
| `Tn_state.Transfer` | value transfer (revm `transfer` + `deduct_caller`) | done | A value-transfer transaction plus its pure state transition `apply : World_state → t → (World_state, error) result`, the first engine behind the `ENGINE` seam to carry a real `error` (`Nonce_mismatch` / `Insufficient_balance` / `Balance_overflow`) in place of `Noop`'s uninhabited one. Exact-nonce and balance checks; the recipient is credited from the already-updated state so a self-transfer nets to zero and still advances the nonce; any error reverts atomically. **Deferred inclusion divergence:** `Nonce_mismatch` and `Insufficient_balance` are reth's pre-execution rejections (no state change — faithful); `Balance_overflow` is a mid-execution failure that reth includes as a failed tx (nonce advanced, gas charged, value reverted). This gasless layer reverts it atomically; the included-and-failed nonce advance + fee charge belong with the gas / block-execution chunk. Not yet wired into the running slice (awaits batch payloads, networking-deferred). |
| `Tn_evm.Alu` | `revm-interpreter/src/instructions/{arithmetic,bitwise}.rs` | done | The **total** EVM semantics of every arithmetic, comparison, bitwise and shift opcode, over the honest (partial-on-zero-divisor) `U256` arithmetic: `DIV`/`MOD` by zero are zero, `SDIV`/`SMOD` read two's complement (divide magnitudes, re-sign by the sign exclusive-or — the lone `-2^255 / -1` overflow falls out unforced), `ADDMOD`/`MULMOD` reduce the true 257-/512-bit intermediate, `SIGNEXTEND`, the one-or-zero comparison words, `BYTE`, and `SHL`/`SHR`/`SAR` with their 256-bit boundaries. Pure functions of their word operands — no gas, no stack, no state. Held to an independent zarith oracle by 25 property tests. |
| `Tn_evm.Opcode` (+ `Depth`) | `revm-bytecode/src/opcode.rs` (the `opcodes!` table) | done | The instruction set as a sum type, with `decode`/`to_byte` round-tripping over all 256 bytes and `immediate_bytes` for the `PUSH` widths. Every operand a byte can carry is a smart-constructed newtype — `Push_bytes` (1–32) and `Depth` (1–16) — so `PUSH33` or `DUP17` is unrepresentable rather than rejected at run time. **Scope divergence:** the *single-frame* subset is defined — arithmetic, comparison, bitwise and shift (dispatched to `Alu`), stack, memory, gas and control flow, plus, since the host seam, the block and transaction context (`COINBASE`…`BASEFEE`, `0x41`–`0x48`; `ORIGIN`/`GASPRICE`), the frame's own context and calldata and code (`ADDRESS`…`GASPRICE`, `0x30`–`0x3a`), account storage (`SLOAD` `0x54`, `SSTORE` `0x55`) and `MCOPY` (`0x5e`). Since the hash landed it also covers `KECCAK256` (`0x20`), the logs (`LOG0`-`LOG4`, `0xa0`-`0xa4`, one constructor carrying a `Topic_count.t`) and EIP-1153 transient storage (`TLOAD` `0x5c`, `TSTORE` `0x5d`); and, once code landed on an account, the external-code readers (`EXTCODESIZE` `0x3b`, `EXTCODECOPY` `0x3c`, `EXTCODEHASH` `0x3f`, the last carrying EIP-1052's fold-to-zero). Since chunk 35 it also covers the EIP-4844 readers `BLOBHASH` (`0x49`) and `BLOBBASEFEE` (`0x4a`): `BLOBHASH` answers zero for every transaction this chain can carry, because revm gates it on the transaction *type* (`context.rs:485-493`) and the port's transaction sum has no 4844 arm, and `BLOBBASEFEE` reads `Env.Block.blob_gasprice`, which a consensus-faithful caller binds to `Env.Block.consensus_blob_gasprice` = `2^128 - 1`, the value TN's one production path hard-codes (`config.rs:139-142`). Nothing remains absent. What once needed a **second frame** or a piece of state the port had not built — `RETURNDATASIZE`/`RETURNDATACOPY`, `BLOCKHASH`, the calls, the creations and `SELFDESTRUCT` — landed with the call chunk, and the blob readers close the set here. The total decoder is still the mechanism for everything outside it: an unassigned byte decodes to `None`, so a program using one halts rather than silently doing something else, needing no allow-list and no feature flag — which is exactly what keeps `0xEF` an undefined byte and halts the second hop of a delegation chain. `PREVRANDAO` is named for the post-merge reading of `0x44` rather than revm's retained `DIFFICULTY`, because this port targets Prague, where the pre-merge reading is not representable. |
| `Tn_evm.Stack` | `revm-interpreter/src/interpreter/stack.rs` | done | The 1024-word operand stack, immutable and all-or-nothing: the limit is checked *before* a push, and a multi-word pop either takes every word or none. **Deliberate divergence:** revm reports a `DUP`/`SWAP` *underflow* as `StackOverflow` (its stack helper returns a bare bool the instruction wrapper cannot tell apart, `instructions/stack.rs:44-62`); this port reports the condition that actually held. Nothing observable turns on it — every exceptional halt discards the frame, consumes all gas and returns no output, so the reason is diagnostic only. |
| `Tn_evm.Memory` | `revm-interpreter/src/interpreter/shared_memory.rs` | done | Byte-addressed frame-local memory: reads are total (an unwritten byte is zero), expansion is `O(1)` and rounds up to whole 32-byte words, and only nonzero bytes are stored so `equal` is exact content equality. Charging is deliberately *not* here — `words_needed` reports what an access requires and the interpreter pays for the difference, since only it holds the gas. A zero-length access needs no words at any offset, which is the rule `RETURN`/`REVERT` rely on. The host seam added `store_bytes`, the bulk writer for `CALLDATACOPY`/`CODECOPY`/`MCOPY`: it folds `store_byte`, so the canonicity rule that a zero byte *removes* its key is stated once — a copy length routinely exceeds its source length, so writing long runs of zeroes is the common case, not a corner. `MCOPY` needs no temporary buffer and no direction test, because `slice` reads the *old* memory and `store_bytes` writes a new one; revm must reason about overlap direction in a flat buffer (`instructions/memory.rs:81`), a persistent map does not. |
| `Tn_evm.Gas` | `revm-context-interface/src/cfg/gas.rs` + `revm-interpreter/src/instructions.rs` (the static table) | done | The Cancun/Prague schedule: the named tiers (`ZERO` 0, `BASE` 2, `VERYLOW` 3, `LOW` 5, `MID` 8, `HIGH` 10, `JUMPDEST` 1) as a per-opcode static cost charged *before* the instruction body, `EXP`'s 50-per-exponent-byte dynamic cost, and the memory curve `3w + w²/512` charged as the difference against what the frame already paid. The curve is computed **exactly** in 63-bit native `int` by splitting the quadratic term (`w = 512q + r`, so `w²/512 = qw + rw/512`) and checking each product before forming it; it returns `None` only when the true cost really exceeds any representable allowance, so treating `None` as out-of-gas is exact rather than conservative. (revm saturates in `u64`; the two agree on every memory a frame could pay to reach.) The host seam added the EIP-2929/2200/3529 prices, all taking an `Access.warmth` rather than a `bool` so they cannot be applied to a lookup that did not happen: `storage_access_cost` (`SLOAD`'s cold surcharge, 2000 on top of the static 100), `account_access_cost` (`BALANCE`'s, 2500 on top of 100), `copy_cost` (3 per word), `sstore_entry` (the EIP-2200 2300-gas sentry *fused* with the 100-unit static charge, because the sentry tests the allowance before that charge), `sstore_dynamic_cost` and `sstore_refund`. **The `SSTORE` cold surcharge is 2100, not `SLOAD`'s 2000** — revm charges `SSTORE` its cold cost in full rather than net of the warm 100, so the two constants differ by exactly the static charge and are easy to conflate into an undercharge that no single test catches. |
| `Tn_evm.Code` | `revm-bytecode/src/legacy/analysis.rs` (`analyze_legacy`) | done | Bytecode plus its valid jump destinations, computed once by walking the code and stepping over each `PUSH` immediate, so a `0x5b` inside push data is never a destination. Reading a byte is **total**: every offset past the end reads zero (`STOP`). That is the same behaviour revm gets by physically padding the code — a truncated immediate reads zero-extended and a program counter off the end halts — expressed as a total read, so the analysed code stays identical to the code given and the padding can never be jumped to. `window` exposes the code as a `Data.t` for `CODECOPY`, built once in `of_string` beside the jump analysis so a `CODECOPY` in a loop does not rebuild it per iteration. |
| `Tn_evm.Env` | `revm-context-interface/src/block.rs`, `transaction.rs` + `revm-interpreter/src/instructions/{block_info,tx_info}.rs` | done | The context a frame reads but cannot change, threaded *beside* the machine as an argument exactly as `Code.t` is. Split into `Block` / `Tx` / `Call` **by lifetime**, which is load-bearing rather than tidy: block fields are fixed for every frame of every transaction in the block, `ORIGIN` and `GASPRICE` for every frame of one transaction (revm sources `ORIGIN` from the transaction's sender, not the immediate caller, `instructions/tx_info.rs`), and call fields change per frame. Because `with_call` is the only producer of a changed `t`, the call chunk is structurally unable to rebind `ORIGIN` when entering a sub-frame; the classic port bug made a compile error. `CHAINID` is folded into `Block` (revm reads it from config) since a one-field third record would enforce nothing. Two fields are **deliberately absent**: no static-call flag, so `SSTORE`'s `require_non_staticcall` guard (`instructions/host.rs:229`) has nothing to test and is not written; and no call depth or return-data buffer. All three arrive with the call chunk. Step 31 adds one non-EVM field to `Block`: `basefee_address`, the settlement sink `TNEvmHandler` reads per transaction (`evm/handler.rs:28-44`); no opcode sees it, it rides here for the same reason `CHAINID` does. |
| `Tn_evm.Data` | `revm-interpreter/src/instructions/system.rs` (`as_usize_saturated!` on copy sources) | done | A read-only byte window with the EVM's out-of-range rule in one place: reading past the end yields zeroes, and a source offset too large to *be* an offset yields all zeroes rather than failing. revm converts a copy's **source** offset with `as_usize_saturated!` and zero-fills the shortfall (`system.rs:174` calldata, `:92` code) while converting the **length** and **destination** with `as_usize_or_fail!` (`:83`, `:164`, `:261`), so a source offset of `2^255` is not an error — it reads nothing — and only the length and destination can halt a frame. One type serves calldata and code because `CALLDATACOPY` and `CODECOPY` are priced identically (`Gas.copy_cost`) and specified identically; sharing the reader is how this port stops them drifting apart. |
| `Tn_evm.Access` | `revm-context-interface/src/journaled_state.rs` (`StateLoad`, `warm_*`) + `journaled_state/entry.rs` (`AccountWarmed`/`StorageWarmed`) | done | The EIP-2929 access set: which addresses and which (address, slot) pairs this transaction has touched, and therefore what a touch costs. The `warmth` witness is **abstract and constructorless** — the only way to obtain one is `touch_account`/`touch_slot`, which necessarily return the grown set alongside it — and since `Gas.account_access_cost`, `Gas.storage_access_cost` and `Gas.sstore_dynamic_cost` accept nothing else, a cold surcharge can never be computed from a guess, a stale flag or a lookup that did not happen. `mem_account`/`mem_slot` are safe to expose precisely because they return `bool`, a type no price accepts. **Nothing is pre-warmed:** EIP-2929's origin and callee, EIP-3651's coinbase and EIP-2930's access list are the transaction layer's business and arrive through `of_transaction`, so `empty` honestly means every first touch is cold. Reverting needs no code here — revm un-warms by replaying `mark_cold` (`entry.rs:317-319`, `:391-398`); the set is persistent and lives inside `Effects.t`, so a reverting frame simply never hands it back. |
| `Tn_evm.Sstore_state` | `revm-context-interface` `SStoreResult` (`context.rs`) | done | The three words EIP-2200 prices an `SSTORE` against — `original` (the pre-transaction value), `present` (the current value) and `updated` (the value being written) — plus `classify : t -> change` over the closed `No_op \| Fresh_set \| Fresh_reset \| Dirty`. Making the classification a sum type rather than a chain of conditionals is what lets `Gas.sstore_dynamic_cost` and `Gas.sstore_refund` match exhaustively on the same four cases, so the cost table and the refund table cannot fall out of step. |
| `Tn_evm.Refund` | `revm-interpreter/src/gas.rs` (`Gas::refunded`, an `i64`) | done | The EIP-3529 refund accumulator. **Signed on purpose:** `SSTORE` credits for freeing a slot and claws it back when the slot is dirtied again in the same transaction, so clearing for +4800 and rewriting for -4800 dips the running total below where it started — revm's field is an `i64` for exactly this reason, and its non-negativity comment is about the *final* value. That is why it is not a field of `Gas.t`: an allowance is non-negative by construction and this is not, so folding them would force one invariant to be a lie, and there is no arithmetic between the two types at all. It lives inside `Effects.t` because it *is* an effect — one dropped value discards a reverted frame's refunds along with its writes. **The EIP-3529 one-fifth cap is deliberately absent:** it is a property of a whole transaction's `spent`, not of one frame, so applying it per frame would be arithmetically wrong; the clamp belongs to the transaction chunk, where `to_int` is consumed. |
| `Tn_evm.Effects` | `revm-context-interface/src/journaled_state.rs` (`Journal`) + `journaled_state/entry.rs` (`JournalEntry`) | done | Everything a frame can change outside itself, as one persistent value: the world state, the `Access.t` and the `Refund.t`. **This is the module where revm's mutable undo journal has no counterpart.** revm needs one because its state is a mutable map with no older version left to return to; here all three components are persistent, so the value that *was* the state is still in the caller's hand. A checkpoint is a binding, a revert is using the old binding, an exceptional halt is dropping the new one — undoing is not an operation but the absence of one, and every class of bug in which an undo entry is pushed for one change and forgotten for another is absent because there is no undo code. `'a load` bundles a read's value, its `Access.warmth` and the effects with that access recorded, so a price can never be attached to the wrong read; `plan_store`/`commit_store` split an `SSTORE` into pricing and commitment so the cost is computed before the write lands. `start` fixes the pre-transaction world once and offers no way to replace it, which makes EIP-2200's `original` transaction-scoped **by construction** — exact while a run is one frame, and the one assumption `CALL` will invalidate, at which point `original` moves onto the slot as revm has it. |
| `Tn_evm.Interpreter` | `revm-interpreter/src/interpreter.rs` (`run_plain`/`step`) + `instructions/{stack,memory,control,system,host,tx_info,block_info}.rs` | done | The machine: `run : env:Env.t → code:Code.t → gas:Gas.t → effects:Effects.t → outcome`, pure and **total**. Decode, charge the static price, dispatch; the program counter pre-advance is folded into an explicit next-offset so `PC` pushes its own offset as in revm. `effects` is both the input world and the output accumulator, so a caller that keeps its own copy holds the pre-frame state for free. Faithful on the details that bite: `JUMPI` branches on any *nonzero* condition and does not validate a destination it does not take; a zero-length `RETURN`/`REVERT` ignores its offset entirely; `MSIZE` reports whole words; `GAS` reports the balance after its own cost; `SSTORE`'s stack pop precedes the EIP-2200 sentry. **Nothing is pre-warmed** — the transaction layer builds the `Access.t` that `Effects.start` takes, and passing `Access.empty` models a frame in which even the executing account is cold, which is observable on `SELFBALANCE`. **Termination is typed, not policed:** every instruction that continues costs at least one gas, so a finite allowance bounds the run — no step limit exists or is needed (a test pins the invariant that only the four halting instructions are free, and a property test pins termination under `SSTORE`, whose refunds never re-fund the running allowance). The `outcome` encodes the gas *and* the effect rule: `Stopped`/`Returned`/`Reverted` carry the surviving allowance, `Failed` carries **none** (a frame that halts exceptionally forfeits the whole allowance at the frame boundary, `revm-handler/src/frame.rs:478-488`), and **only `Stopped` and `Returned` carry `Effects.t`** — so "a revert discards its writes, its warmings and its refunds" is a fact of the type rather than a rule the code has to remember. |
| `Tn_evm.Transaction` | `revm-context/src/tx.rs` (`TxEnv`) + `revm-context-interface/src/transaction.rs` | done | The typed transaction the executor consumes: pre-recovered `sender`, `Nonce`, `gas_limit`, a `kind` (`Call of address` \| `Create`), `value`, `data`, an EIP-2930 `access_list`, an optional `chain_id`, and a `fee` sum (`Legacy`/`Access_list` sharing one `gas_price`; `Dynamic` with `max_fee` and optional `max_priority`). `effective_gas_price ~base_fee` is revm's default trait method (`transaction.rs:147-160`): `gas_price` for Legacy/2930, else `max_fee` when there is no tip or `min(max_fee, base_fee + priority)`. Sender crypto-recovery is deferred (matches `Transfer`); since chunk 33 the fee sum has a fourth arm, `Set_code` (type 4, EIP-7702), carrying a *required* `max_priority`, a bare `target` address (a set-code creation is unrepresentable) and the signed authorization list; only EIP-4844 still has no field. |
| `Tn_evm.Intrinsic` | `revm-context-interface/src/cfg/gas_params.rs` (`GasParams::initial_tx_gas`) | done | The initial transaction gas and the EIP-7623 floor, pure. Tokens are `zero_bytes + 4 * nonzero_bytes` (EIP-2028's 4/16-per-byte split); `initial_gas = tokens*4 + access_list(2400/addr + 1900/slot) + 21000`, plus `32000 + 2*num_words(initcode)` for a creation (`num_words = div_ceil 32`, `num_words 0 = 0`); `floor_gas = tokens*10 + 21000`, calldata only. Constants pinned to revm 22.1.0/14.0.0. |
| `Tn_evm.Receipt` | `revm-context/src/result.rs` (`ExecutionResult`) | done | The outcome of an *included* transaction: `Success` (output, created address, gas used net of refund, gas refunded, logs) \| `Reverted` (output, gas) \| `Halted` (a `halt_reason`, gas = the whole limit). All three are committed; a validation rejection is categorically different and produces no receipt (it is `Executor.execute`'s `Error`), so the type cannot represent one. `halt_reason` names the create terminal faults (EIP-684 collision, EIP-3541 reserved prefix, EIP-170 size, deposit OOG) distinctly from a `Frame_halt` carrying an `Interpreter.error`. |
| `Tn_evm.Executor` | `revm-handler/src/handler.rs` (`run` / `pre_execution` / `execution` / `post_execution`) | done | The Prague single-transaction STF of revm's Ethereum-mainnet *handler* ("mainnet" names revm's handler, never TN mainnet, which runs Shanghai; the fork scope is stated once, in `lib/evm/env.mli`): `execute : World_state → block → Transaction → (Receipt * World_state, error) result`. Validation (chain id; per-type fee vs base fee; block gas limit; EIP-3860 initcode; intrinsic and EIP-7623 floor vs the limit; EIP-3607 sender-has-code **before** the nonce; balance vs `gas_limit*max_fee + value`); deduct at the **effective** price while the check used the max, nonce bumped for a call and inside the frame for a creation; the EIP-2929/2930/3651 warm set; the first frame (a call, a creation, or a **top-frame precompile**) dispatched exactly as `do_call` does (`interpreter.ml:1219-1239`), without which a direct tx to e.g. IDENTITY would run empty code and diverge; gas normalized per outcome; the EIP-3529 one-fifth cap **before** the EIP-7623 floor; then **telcoin's settlement, not mainnet's** (step 31): `TNEvmHandler`'s two overrides (`evm/handler.rs:46-172`) dock the caller's refund by `Gas_penalty`'s inefficiency penalty priced on the **pre-refund** spend, credit that penalty and `basefee * gas_used` to the block's `basefee_address` (`Env.Block.basefee_address`, default `System_contracts.governance_safe_address`) instead of burning the base fee, pay the beneficiary the priority fee alone, and skip settlement wholesale for a `SYSTEM_ADDRESS` sender (`handler.rs:85-87, 145-147`). The upfront charge stays stock revm and receipts are penalty-free, so `gas_used` figures are mainnet-identical. Success/revert/halt all commit (nonce + fee persist; revert/halt roll back value, storage, logs); a rejection changes nothing. EIP-161 pruning is by construction (every credit goes through `World_state.set_account`, which drops an `is_absent` account). Since chunk 33 the EIP-7702 authorization loop runs in pre-execution (25000 intrinsic per *listed* entry, the 12500-difference refund folded in after the outcome match, delegations surviving revert and halt) and the depth-0 frame resolves a delegation designator through `Call_target`, exactly as the call opcodes do; only EIP-4844 blobs stay out of scope; the state root is now `Tn_evm.Block_roots.state_root`. |
| `Tn_evm.Gas_penalty` | `crates/tn-reth/src/evm/utils.rs:17-84` (`calculate_gas_penalty`) | done | The gas-limit inefficiency penalty, transcribed step for step in `Z` (the `u128` intermediates reach `10^16 * unused`, past 63-bit int): limit at or below `210_000` exempt; `ratio = 10^9 * spent / limit` at or above `10^8` (ten percent) exempt; else `(10^8 - ratio)^2 * unused / 10^16`. Fed the **pre-refund, post-floor** spend by `Executor` exactly as `reimburse_caller` reads `gas.spent()`. The proven postcondition `penalty <= unused` makes the `Z` narrowing total and leaves Rust's `unwrap_or(unused)` fallback (`utils.rs:83`) unported as dead code; the only totality delta is a clamp of negative spends to zero, `u64`'s lower bound made explicit. Vectors are telcoin's own `#[cfg(test)]` tables. |
| `Tn_evm.Bloom` | `alloy-primitives` `Bloom` (m3:2048) | done | The 2048-bit (256-byte) logs bloom, a verbatim port of alloy-primitives. An immutable set of set-bit indices, so accrual is a pure union and serialization a pure `String.init` gather with no in-place buffer. Each input sets three bits read as big-endian 2-byte pairs of its keccak256 at offsets {0,2,4} masked to 0..2047, placed at byte `255 - bit/8` with mask `1 lsl (bit mod 8)`; the two easily-inverted details are held to alloy's own `works` vector. `of_logs` ORs the address and each topic of every log, each hashed separately. |
| `Tn_evm.Receipt_envelope` | `alloy-consensus` `ReceiptWithBloom` (`eip2718_encode_with_bloom`) | done | The verbatim EIP-2718 receipt leaf value: `[status, cumulative_gas_used, bloom, logs]` in that order, legacy as the bare RLP list and typed (1/2) prefixed with exactly one type byte and no outer string wrapper (the network form the trie must not use). `status` is EIP-658 (true only for `Success`); the 256-byte bloom is derived from the receipt's own logs at encode time, since `Receipt` stores neither a bloom nor a tx type. A log encodes as `[0x94||addr, list of 0xa0||topic (fixed 32B), data]`. |
| `Tn_evm.Block_roots` | `alloy-trie` `state_root_unhashed` / `storage_root_unhashed` / `ordered_trie_root` | done | The four block-header roots plus the agreement oracle, as functions over `World_state.t` / `Account.t` (they live here rather than on those types because `tn_state` sits below `tn_trie`). `state_root` is `secure_root_of` over `(20-byte address, account_rlp)` with a nested per-account `storage_root`; `storage_root` is `secure_root_of` over `(32-byte slot, scalar-RLP value)` with zero slots already absent; `receipts_root` threads the net cumulative-gas prefix-sum through `Receipt_envelope` and roots the verbatim 2718 bytes; `transactions_root` roots verbatim 2718 envelopes and `transactions_root_of` roots `Tx_envelope.t` values the port encodes itself (chunk 29); `agree` is state-root equality. Every root is byte-checked against an out-of-tree alloy oracle (alloy-trie 0.9.5, alloy-consensus 2.1.0, alloy-primitives 1.6.0) and alloy/reth vectors, each load-bearing test mutation-confirmed. **State-root fidelity precondition:** reth also assumes EIP-158/161 touched-empty clearing has been applied by the execution layer; the port prunes on `is_absent` and still defers that clearing, so a touched-empty account holding nonzero storage would be kept. That gap is upstream of the root code, not a bug in it. |
| `Tn_evm.Tx_payload` / `Tx_signature` | `alloy-consensus` 1.8.3 `TxLegacy`/`TxEip2930`/`TxEip1559`/`TxEip7702`, `alloy-primitives` 1.5.7 `Signature` | done | The unsigned transaction and its signature. Fields are declared in **wire** order, not alloy's struct order (alloy declares `max_fee` before `max_priority` and `access_list` before `input`; both are wire-inverted, and both fee fields share a type, so mirroring the struct compiles and silently inverts the fee semantics). `t` is abstract behind four smart constructors, so a legacy transaction with an access list and a typed one without a chain id have no values; `variant` hands the encoder the per-type fields already discriminated, so no layout is reached by defaulting an absent field. Legacy `v` via `to_eip155_value`/`of_eip155_value` (u128-exact arithmetic, accepts only 27/28/≥35 with the chain id bounded by `u64::MAX`, rejects the 0/1 that `normalize_v` would take — that helper is on the 65-byte raw-signature path, not this one). The signature is unvalidated by design: `r = 0`, `s = 0` and `s ≥ n` all decode, exactly as alloy's do. |
| `Tn_evm.Tx_envelope` (+ `Eip2718`) | `alloy-consensus` `RlpEcdsaEncodableTx`/`RlpEcdsaDecodableTx`, `alloy-eips` 1.8.3 `Encodable2718`/`Decodable2718` | done | Signing payload, verbatim 2718 envelope, transaction hash, strict decoder. Legacy carries no type byte and no outer header; typed prepends one raw byte through `Eip2718.frame`, the single copy of that rule shared with `Receipt_envelope`. The signature tail is `v, r, s` inside the same flat list, and `r`/`s` are RLP **scalars** — an `r` below `2^248` shortens the envelope and changes the hash. The network (p2p) form is deliberately absent, as it is for receipts. **The accept set is canonical framings only, which is narrower than alloy's:** `0x00 ‖ legacy` and `0xb8 ‖ len ‖ typed` are rejected here (`Zero_type_byte`, `Outer_string`) and accepted upstream, where they re-hash into their canonical twins — a consequence of alloy's variant-derive retrying over an unrestored buffer, not of a rule. The cost (a peer-crafted non-canonical envelope reth would accept is refused; latent while networking is deferred) and the gain (`encode_2718 ∘ decode_2718 = id` on the nose) are both stated in the `.mli`. One value narrowing: a nonce or gas limit above `Int.max_int` is `Unrepresentable_scalar`, on transactions no block could execute; `hash_of_2718` still works from the bytes. Byte-checked against an out-of-tree alloy oracle (alloy-consensus =1.8.3, alloy-eips =1.8.3, alloy-primitives =1.5.7, alloy-rlp =0.3.13) whose first fixture is the EIP-155 specification's own published example, with each load-bearing rule mutation-confirmed. |
| `Tn_evm.Tx_recovery` / `Public_key` | `alloy-consensus` `SignerRecoverable`, `crypto::secp256k1::recover_signer` | done | The EIP-2 low-`s` gate (strict `>` `SECP256K1N_HALF`, so `s = floor(n/2)` is legal — the one signature that distinction decides is a pinned test), the digest re-derived from the payload rather than the received bytes, the raw 0/1 parity as the recovery id, `r`/`s` fixed-width here unlike the RLP path, and `keccak256(64-byte tag-stripped key)[12..]` — the last shared with `ECRECOVER` through `Public_key` rather than duplicated. The bound itself is `Secp256k1.s_is_high`, derived from the group order that module already held, so the constant is written down once. No unchecked variant is exposed: alloy's normalises `s` and flips the recid under k256, returning the same address for a malleation, and telcoin never calls it. `r`/`s` zero-or-out-of-range is left to the curve primitive's own guard rather than duplicated in front of it. |
| `Tn_evm.Block_execution` / `Block_header` / `System_call` (+ `Block_context`, `Block_gas`, `System_contracts`, `Batch_position`, `Epoch_boundary`, `Withdrawal`, `Hash32`) | `crates/tn-reth/src/evm/block.rs` (`TNBlockExecutor`, `TNBlockAssembler`), `evm/mod.rs:164-221` (`transact_system_call`) | partial | The block spine: `apply_pre_block` (EIP-4788 gated on `first_batch`, then EIP-2935 **ungated**, both skipped at block zero), the per-transaction left fold over `Executor.execute`, and the twenty-one-field header. System calls run the **real** alloy-eips 1.8.3 runtime bytecode through `Executor.execute` on a synthetic Legacy tx (caller `SYSTEM_ADDRESS`, nonce = the live nonce, 30M gas, price 0, basefee swapped to 0), telcoin's own pipeline rather than revm's `run_system_call` fast path, and commit `retain(target)`, which is load-bearing: the caller's nonce bump must not reach the trie, and dropping it is what lets both calls in one block see nonce 0. `Pre_block.t` is a constructorless phase token carrying the post-pre-block world, so the fold cannot run first; the outcome carries the envelopes **and** the receipts, so the two roots commit one list. The header takes no `state_root` and no transaction list: both come from the outcome. The EIP-2935 write slot is `(number - 1) mod 8191` (the window is 8191, not 8192) and EIP-4788 writes the pair `(ts mod 8191, ts mod 8191 + 8191)`; both are pinned by end-to-end tests over the real bytecode, and the 21-field header order is certified against the published hash of mainnet block 23068672. **Scope divergence (closed in chunk 32):** the epoch-close machinery (`apply_consensus_block_rewards`, `apply_closing_epoch_contract_call`, `shuffle_new_committee`) now runs in `Block_execution.finish`, whose Closing arm derives the `applyIncentives` pairs from the boundary's withdrawal records (disclosed single-sourcing hardening; identical values on every input Rust produces) and runs `Epoch_close.close` with the boundary randomness; `Block_header.assemble` takes the `Finished.t` it produces, so a closing world is rooted only after the close and the `Epoch_close_state_deferred` variant is deleted. Still absent: the adiri-only `apply_consensus_registry_fork` (cfg-gated, placeholder `u32::MAX` trigger, cut not deferred) and `merge_transitions` (reth bundle bookkeeping with no image here). **Deliberate divergence:** the Cancun/Prague timestamp gates are absent, because the port has no fork schedule and models TN's *testnet* configuration, which activates both at timestamp 0 (`chain-configs/testnet/genesis.yaml:15-17`); TN mainnet's committed genesis declares `shanghaiTime` alone, so on mainnet this port would make two system calls telcoin skips (the fork scope is stated once, in `lib/evm/env.mli`). **Deliberate divergence:** `assemble` computes the state root from the executor's world instead of taking it as an input, which makes "this header's state root is the root of the state this block produced" a fact of the type. **Deliberate divergence:** an invalid transaction is an **error**, not a skip. Telcoin's block executor errors (`block.rs:879-882`) while its *builder* logs and continues (`tn-reth/src/lib.rs:783-800`); a builder that drops transactions belongs above this module and would hand it a shorter list. **Fee accounting (closed in step 31):** the fold routes each transaction through `Executor.execute`, which now implements `TNEvmHandler`'s split (penalty docked from the refund, penalty + base fee credited to `Env.Block.basefee_address`, system senders skipped), so the fee divergence this row disclosed through step 30 is gone: post-state, `stateRoot` and header hash agree with telcoin's for nonzero base fees and for over-declared gas limits. |
| `tn_rlp` (`Rlp`) | `alloy-rlp` 0.3.13 | done | RLP encode/decode, ported byte-for-byte. The five encoding forms; `encode_bytes` (the `[u8]` rule: a one-byte string `< 0x80` is itself, leading zeros preserved) kept distinct from `encode_scalar`/`encode_nat` (minimal big-endian, zero → `0x80`), so the number zero and the byte `0x00` never collide; a canonicity-enforcing `decode`/`decode_exact` (non-canonical single byte, non-canonical long-form size, leading-zero length, overflow, truncation) over a `Str`/`List` tree. A leaf codec (no keccak/trie dependency) so the later receipt, transaction and header chunks reuse it. |
| `tn_trie` (`Trie`, + internal `Nibbles`/`Hex_prefix`/`Node`) | `alloy-trie` 0.9.5, `nybbles` 0.4.8 | done | The Merkle-Patricia-Trie root over a complete key/value set. A recursive hexary builder (leaf / extension / 17-slot branch whose slot 16 holds the value of a strict-prefix key), hex-prefix path encoding, and the strict `< 32` child collapse (inline RLP else `0xa0 || keccak`) with the root always hashed. `root` (general, `Duplicate_key` on a repeated key), `secure_root_of` (keys keccak-hashed — the state and storage tries), `ordered_trie_root(_rlp)` (keys `RLP(index)` via `adjust_index_for_rlp` — the transactions and receipts roots), and `account_rlp` (`[nonce, balance, storage_root, code_hash]`). The eleven `ethereum/tests` trie fixtures pass; every tested root (ordered, state and storage) is independently reproduced by an out-of-tree alloy-trie 0.9.5 oracle, each load-bearing rule confirmed by mutation. |
| `Tn_batch.Tx_shape` | reth v1.11.3 `recover_raw_transaction` = alloy 1.8.3 `decode_2718_exact` + checked `recover_signer` | done | The TN-mirror batch wire decoder, a SEPARATE decoder from `Tn_evm.Tx_envelope` on purpose: it accepts the 0x00-tagged legacy framing (empirically pinned against the vendored crates) and full-u64 nonce/gas_limit scalars, and decodes well-formed EIP-4844 envelopes, so validator rules 5/6/7 keep TN's exact error identities; strict EIP-2 high-s gate (reth routes to CHECKED recovery); three-arm error enum mirroring reth `EthApiError` (reth discards the finer alloy taxonomy). Drift against `Tx_envelope` is pinned by a differential test over the whole existing tx fixture corpus. |
| `Tn_batch.Batch_validator` | `BatchValidator::validate_batch` (`batch-validator/src/validator.rs:39-82`) + worker `network/error.rs:83-103` | done | The eight rules in TN's exact order, short-circuiting on the FIRST failure; all 12 error variants in Rust declaration order (the two dead arms kept so the penalty map stays total and 1:1); the peer-penalty map ported as a pure exhaustive function; `Valid.t` witness deliberately NOT demanded by the execution seam (TN's is_certified bypass). Type-4 (EIP-7702) PASSES validation, TN's known gap, pinned by test. **Deferred:** `submit_txn_if_mine` (dead upstream); penalty ENFORCEMENT (peer scoring is a networking-chunk concern). |
| `Tn_batch.Output` | `ConsensusOutput`/`CertifiedBatch` + the subscriber payload fetch (`executor/src/subscriber.rs:430-551`, `types/src/primary/output.rs:31-104`) | done | Payload attachment made pure: the worker fetch becomes an injected `lookup`, the authority execution-address table becomes `address_of`; a missing digest (`MissingFetchedBatch`) and an unknown authority are the two fatal arms; a digest under two certificates resolves TWICE (subscriber clone semantics; the adiri dup-batch carve-out is not ported); `close_epoch` is never stored, `closes_epoch` recomputes `committed_at >= boundary` (run_epoch.rs:553-556) as upstream demands of deserializers. |
| `Tn_batch.Block_plan` | `execute_consensus_output` (`engine/src/payload_builder.rs:99-224`), `TNPayload` (`tn-reth/src/payload.rs:30-120`) | done | One spec per flattened batch, pure data: certificate address as beneficiary (never the batch's own field), the batch's own base fee, the fork-blind 30M gas limit (never the batch's sum), `output_digest XOR batch_digest` mix hash, the packed `Batch_position` difficulty word, close flag only on the LAST spec of a closing output; plus `Skip` and `Close_block` for the two empty-output behaviors. The close block's base fee/gas limit are parent-header state only the engine chunk holds, so `Close_spec` deliberately omits them. |
| `Tn_batch.Batch_payload` | `build_block_from_batch_payload`'s drop phase (`tn-reth/src/lib.rs:808-848`) | done | The engine-side drop layer: filter and continue, never error, so every honest node derives the same block from the same certified bytes (issue #933); sits ABOVE `Tn_evm.Block_execution`, which errs instead of skipping by contract. **Deliberate divergence:** a well-formed EIP-4844 envelope or a nonce/gas limit in [2^62, 2^64) is dropped HERE, where TN executes it and then skips at the InvalidTx arm (`lib.rs:855-866`); the produced block CONTENT is identical, the equivalence is argued and test-pinned rather than differentially run, and any future differential gate against a Rust node must include one such batch. **Closed by the engine chunk:** the `ENGINE.execute` widening this row once promised as `t -> Output.t -> (t * block list, error) result` was not writable in that literal form — `lib/execution` sits *below* `tn_batch` and cannot name `Output.t` — so the seam abstracts `output` and `Tn_engine.Engine` instantiates it at `Tn_batch.Output.t`; `Env.Block`/`Block_context` construction and the `Epoch_boundary` commitment (withdrawals plus the leader-signature randomness, converted rather than recomputed) landed with it. **Still deferred:** `ReportBatch`/`SyncFrame`/gossip wire enums + snappy/length framing (networking; the gap-5 wire facts are recorded in the chunk-34 ground truth); `max_batches_per_output` storage bound. |
| `Tn_engine.Engine` (+ `Config`, `Anchor`, `Recent_hashes`, `Block_number`, `Executed_block`) | `execute_consensus_output` / `execute_payload` (`engine/src/payload_builder.rs:19-308`), `TNBlockExecutor` (`tn-reth/src/evm/block.rs`) | done | The engine link: one committed `Output.t` in, the execution blocks it produces out, satisfying `Tn_execution.Engine.ENGINE` by an `include` rather than by comment, so drift in the seam is a build error here. Folds the plan's specs **as given** — certificate-major, batch-minor — chaining `number`/`parent_hash` block to block, threading the finished world (not the outcome's) into the next block, reading the `BLOCKHASH` window *before* a block and pushing the assembled hash *after* it, so that `BLOCKHASH(n-1)` answers the parent rather than block `n` itself (a block reading its OWN height already reads zero regardless of the ordering, because `Block_hashes.lookup` answers zero below depth one), and stamping the output's commit timestamp, nonce and consensus root on every block of it. The leader count is incremented as the **first statement** of `execute`, ahead of the boundary recompute and of the three-way branch, so a skipped output still pays its leader (`payload_builder.rs:40` runs before the empty test at `:99`). An output is **all or nothing**: the per-block state is a `building` accumulator settled back only on full success, which is upstream's reorg to the pre-output anchor expressed as a value. `Anchor` carries exactly the four facts telcoin reads off `canonical_header` (hash, number, base fee, gas limit) so no genesis header is ever manufactured — `Block_header.assemble` stays the only producer of a header. `Recent_hashes` is prepend-only and capped at 256, so newest-first is a property of the type. The epoch is a **phase** (`Running` / `Sealed`) inside this engine and not a member of the shared seam: the closing output's last block carries the `Epoch_boundary.Closing` commitment, the engine seals behind it, and `begin_epoch` clears the counts, installs the next committee and refuses a boundary that does not strictly advance. The builder's skip stance is real: an `InvalidTx` is recorded on the block and skipped, every other executor error is fatal. **Deferred:** the post-`concludeEpoch` registry reader, so `begin_epoch`'s committee and boundary are the caller's to supply; telcoin's per-block `inc_block` gas tally, which nothing here reads. (The driver deferral this row once carried is closed by `Tn_driver` below.) |
| `Tn_driver` (`Driver` / `Subscriber` / `Batch_store` / `Address_book` / `Chain_spec` / `Outcome`) | the subscriber's mint-and-fetch (`executor/src/subscriber.rs:367-371,430-551`), the epoch manager's entry/close and watermark (`tn-reth/src/lib.rs:1832-1913`, `consensus/src/run_epoch.rs:140-145,547-567,883-908`), `chain-configs/*/genesis.yaml` | done | The execution driver (chunk 37): a pure, fail-stop, two-chain fold that owns the consensus-chain accumulator (`Tn_execution.Consensus_chain`, which `Engine.Noop` shares so the port has ONE fold) and the engine, and never owns the world. One `step` mints `(parent, number)` BEFORE any body is touched, attaches through `Batch_store` - an immutable by-digest map whose key is DERIVED inside `add` (a key/body disagreement is unrepresentable), non-consuming so a digest under two certificates legally resolves twice - executes, and advances the forwarding watermark (the consensus NUMBER recovery keys on) only AFTER the execute succeeds. Every terminal error returns the contiguous executed prefix and NO driver (`Outcome.Halted` has no driver field), so resume-past-a-violation is unrepresentable. The epoch is a `Running`/`Sealed` phase: `begin_epoch` refuses a running driver (the guard the engine deliberately lacks), refuses a committee whose epoch does not strictly advance (the `InvalidPackEpoch` analogue), then hands the engine `closed_at + duration` - the boundary anchored to the closing COMMIT, overshoot kept. `Chain_spec` is the cited stand-in for `genesis.yaml` (chain id 2017/487, 30M gas limit, base fee 7, strictly-positive `Epoch_duration`); the registry alloc stays caller-supplied, the genesis hash a required argument documented as unverifiable in-port, and `create` seats registry + predeploys so a spec lacking them is unconstructible. The sim gained an optional `?batches` injection plan (absent by default, and absent means the pre-injection golden replays byte-identically). **Deferred:** the engine-side resume (`Consensus_chain.resume` exists and waits on an engine counterpart plus a durable body store); the registry reader that would replace `epoch_duration`'s one call site. |
| `Tn_network` | `crates/network-libp2p`, `crates/network-types` | planned | Message types behind `Command`/`Event`. No mature OCaml libp2p exists — largest open risk (see README). |
| `Tn_storage` | `crates/storage` (`tn-storage`, 14 tables) | planned | Persistence signature; MDBX has no OCaml equivalent (candidate: `irmin`). |
